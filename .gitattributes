# 设置默认行为，以防人们没有设置 core.autocrlf
* text=auto

# 明确声明应该规范化的文本文件
*.json text
*.md text
*.bat text
*.cmd text
*.sh text eol=lf
*.vim text
*.conf text

# 不要修改这些文件的行尾符号
*.png binary
*.jpg binary
*.gif binary 